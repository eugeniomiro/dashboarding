<UserControl x:Class="WpfApplication.Demonstrators.Dial360Demonstrator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:db="clr-namespace:Codeplex.Dashboarding;assembly=Codeplex.Dashboarding.Wpf"
    xmlns:bd="clr-namespace:WpfApplication.Demonstrators"
    >

    <Grid x:Name="LayoutRoot" >
        <Grid.RowDefinitions>
            <RowDefinition Height="24"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="160*"/>
        </Grid.ColumnDefinitions>

        <bd:BoundData Grid.ColumnSpan="2" x:Name="_slide"/>

        <db:Dial360 Value="{Binding Path=CurrentValue, Mode=TwoWay}" Minimum="{Binding Path=MinValue}" Maximum="{Binding Path=MaxValue}"  x:Name="_dial0" Grid.Row="1" ValueTextColor="Yellow">
            <db:Dial360.FaceColorRange>
                <db:ColorPoint HiColor="#2A242C" LowColor="#6C0680" Value="0" />
                <db:ColorPoint HiColor="#2A242C" LowColor="#6C0000" Value="55" />
            </db:Dial360.FaceColorRange>
        </db:Dial360>

 
    </Grid>
</UserControl>
